#include <stdio.h>

void main(){
    
    int edad; //llamar variable de tipo entero
    edad=20; //valor que le asigno a la variable
    printf("Edad: %d",edad); //imprime de forma entera la variable asignada
    return; //a la funcion int para retornar la funcion tenemos que usar (0);
}